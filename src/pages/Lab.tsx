import Section from '../components/Section'

export default function Lab() {
    return (
        <Section title="实验室 (Lab)">
            <p>这里用来展示交互 Demo、小实验（Canvas 粒子、WebGL、AI 可视化等）。</p>
        </Section>
    )
}
